# Python bytecode — auto-generated, never commit
__pycache__/
*.pyc
*.pyo

# Checkpoints — large binary files, regenerate by retraining
checkpoints/

# Training outputs
logs/
results.png

# Data files — too large for git
data/
*.parquet
*.csv

# SUMO network and OSM files — large, regenerate via netconvert
*.net.xml
*.osm
*.osm.pbf

# Benchmark output
benchmark_results.png

# macOS junk
.DS_Store
